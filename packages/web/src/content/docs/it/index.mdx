---
title: Introduzione
description: Inizia con HanuBees.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"
import config from "../../../../config.mjs"
export const console = config.console

[**HanuBees**](/) e un agente di programmazione AI open source. E disponibile come interfaccia per terminale, app desktop o estensione per IDE.

![HanuBees TUI con il tema HanuBees](../../../assets/lander/screenshot.png)

Iniziamo.

---

#### Prerequisiti

Per usare HanuBees nel terminale, ti serve:

1. Un emulatore di terminale moderno, come:
   - [WezTerm](https://wezterm.org), cross-platform
   - [Alacritty](https://alacritty.org), cross-platform
   - [Ghostty](https://ghostty.org), Linux and macOS
   - [Kitty](https://sw.kovidgoyal.net/kitty/), Linux and macOS

2. Le chiavi API dei provider LLM che vuoi usare.

---

## Installa

Il modo piu semplice per installare HanuBees e usare lo script di installazione.

```bash
curl -fsSL https://hanubees.com/install | bash
```

Puoi anche installarlo con i seguenti comandi:

- **Con Node.js**

        <Tabs>

      <TabItem label="npm">
      ```bash
      npm install -g hanubees
      ```

          </TabItem>

        <TabItem label="Bun">
        ```bash
        bun install -g hanubees
        ```

          </TabItem>

        <TabItem label="pnpm">
        ```bash
        pnpm install -g hanubees
        ```

          </TabItem>

        <TabItem label="Yarn">
        ```bash
        yarn global add hanubees
        ```

      </TabItem>

  </Tabs>

- **Con Homebrew su macOS e Linux**

  ```bash
  brew install varsansri/tap/hanubees
  ```

  > Ti consigliamo di usare il tap di HanuBees per avere le release piu aggiornate. La formula ufficiale `brew install hanubees` e mantenuta dal team Homebrew e viene aggiornata meno spesso.

- **Con Paru su Arch Linux**

  ```bash
  sudo pacman -S hanubees           # Arch Linux (Stable)
  paru -S hanubees-bin              # Arch Linux (Latest from AUR)
  ```

#### Windows

:::tip[Consigliato: usa WSL]
Per la migliore esperienza su Windows, ti consigliamo di usare [Windows Subsystem for Linux (WSL)](/docs/windows-wsl). Offre prestazioni migliori e piena compatibilita con le funzionalita di HanuBees.
:::

- **Con Chocolatey**

  ```bash
  choco install hanubees
  ```

- **Con Scoop**

  ```bash
  scoop install hanubees
  ```

- **Con NPM**

  ```bash
  npm install -g hanubees
  ```

- **Con Mise**

  ```bash
  mise use -g github:varsansri/hanubees-coding-agent
  ```

- **Con Docker**

  ```bash
  docker run -it --rm ghcr.io/varsansri/hanubees-coding-agent
  ```

Il supporto per installare HanuBees su Windows usando Bun e attualmente in lavorazione.

Puoi anche scaricare il binario dalle [Releases](https://github.com/varsansri/hanubees-coding-agent/releases).

---

## Configura

Con HanuBees puoi usare qualsiasi provider LLM configurando le relative chiavi API.

Se e la prima volta che usi provider LLM, ti consigliamo [HanuBees Zen](/docs/zen).
E una lista curata di modelli testati e verificati dal team di HanuBees.

1. Esegui il comando `/connect` nella TUI, seleziona HanuBees e vai su [hanubees.com/auth](https://hanubees.com/auth).

   ```txt
   /connect
   ```

2. Accedi, aggiungi i dettagli di fatturazione e copia la tua chiave API.

3. Incolla la tua chiave API.

   ```txt
   ┌ API key
   │
   │
   └ enter
   ```

In alternativa, puoi selezionare uno degli altri provider. [Scopri di piu](/docs/providers#directory).

---

## Inizializza

Ora che hai configurato un provider, puoi spostarti in un progetto su cui vuoi lavorare.

```bash
cd /path/to/project
```

E avviare HanuBees.

```bash
HanuBees
```

Poi inizializza HanuBees per il progetto eseguendo il comando seguente.

```bash frame="none"
/init
```

Questo fara analizzare il progetto a HanuBees e creera un file `AGENTS.md` nella root del progetto.

:::tip
Dovresti committare il file `AGENTS.md` del progetto su Git.
:::

Questo aiuta HanuBees a capire la struttura del progetto e gli stili di codice usati.

---

## Utilizzo

Ora sei pronto a usare HanuBees sul tuo progetto. Sentiti libero di chiedergli qualsiasi cosa!

Se e la prima volta che usi un agente di programmazione AI, ecco alcuni esempi che possono aiutare.

---

### Fai domande

Puoi chiedere a HanuBees di spiegarti la codebase.

:::tip
Usa il tasto `@` per fare una ricerca fuzzy dei file nel progetto.
:::

```txt frame="none" "@packages/functions/src/api/index.ts"
How is authentication handled in @packages/functions/src/api/index.ts
```

Questo e utile se c'e una parte della codebase su cui non hai lavorato.

---

### Aggiungi funzionalità

Puoi chiedere a HanuBees di aggiungere nuove funzionalita al progetto. Pero ti consigliamo prima di chiedergli di creare un piano.

1. **Crea un piano**

   HanuBees ha una _Plan mode_ che disabilita la possibilita di fare modifiche e si limita a suggerire _come_ implementera la funzionalita.

   Passaci con il tasto **Tab**. Vedrai un indicatore nell'angolo in basso a destra.

   ```bash frame="none" title="Switch to Plan mode"
   <TAB>
   ```

   Ora descriviamo cosa vogliamo che faccia.

   ```txt frame="none"
   When a user deletes a note, we'd like to flag it as deleted in the database.
   Then create a screen that shows all the recently deleted notes.
   From this screen, the user can undelete a note or permanently delete it.
   ```

   Devi dare a HanuBees abbastanza dettagli per capire cosa vuoi. Aiuta parlargli come se stessi parlando a uno sviluppatore junior del tuo team.

   :::tip
   Dai a HanuBees molto contesto ed esempi per aiutarlo a capire cosa vuoi.
   :::

2. **Itera sul piano**

   Una volta che ti da un piano, puoi dargli feedback o aggiungere piu dettagli.

   ```txt frame="none"
   We'd like to design this new screen using a design I've used before.
   [Image #1] Take a look at this image and use it as a reference.
   ```

   :::tip
   Trascina e rilascia le immagini nel terminale per aggiungerle al prompt.
   :::

   HanuBees puo analizzare le immagini che gli dai e aggiungerle al prompt. Puoi farlo trascinando e rilasciando un'immagine nel terminale.

3. **Implementa la funzionalita**

   Quando ti senti a tuo agio con il piano, torna in _Build mode_ premendo di nuovo il tasto **Tab**.

   ```bash frame="none"
   <TAB>
   ```

   E chiedigli di fare le modifiche.

   ```bash frame="none"
   Sounds good! Go ahead and make the changes.
   ```

---

### Apporta modifiche

Per modifiche piu semplici, puoi chiedere a HanuBees di implementarle direttamente senza dover prima rivedere un piano.

```txt frame="none" "@packages/functions/src/settings.ts" "@packages/functions/src/notes.ts"
We need to add authentication to the /settings route. Take a look at how this is
handled in the /notes route in @packages/functions/src/notes.ts and implement
the same logic in @packages/functions/src/settings.ts
```

Assicurati di fornire abbastanza dettagli, cosi HanuBees fa le modifiche giuste.

---

### Annulla modifiche

Mettiamo che tu chieda a HanuBees di fare alcune modifiche.

```txt frame="none" "@packages/functions/src/api/index.ts"
Can you refactor the function in @packages/functions/src/api/index.ts?
```

Ma ti accorgi che non e quello che volevi. Puoi **annullare** le modifiche usando il comando `/undo`.

```bash frame="none"
/undo
```

HanuBees ora ripristina le modifiche e mostra di nuovo il tuo messaggio originale.

```txt frame="none" "@packages/functions/src/api/index.ts"
Can you refactor the function in @packages/functions/src/api/index.ts?
```

Da qui puoi modificare il prompt e chiedere a HanuBees di riprovare.

:::tip
Puoi eseguire `/undo` piu volte per annullare piu modifiche.
:::

Oppure puoi **rifare** le modifiche usando il comando `/redo`.

```bash frame="none"
/redo
```

---

## Condividi

Le conversazioni che fai con HanuBees possono essere [condivise con il tuo team](/docs/share).

```bash frame="none"
/share
```

Questo creera un link alla conversazione corrente e lo copiera negli appunti.

:::note
Le conversazioni non vengono condivise per impostazione predefinita.
:::

Ecco un'[esempio di conversazione](https://hanubees.com/s/4XP1fce5) con HanuBees.

---

## Personalizza

E tutto qui! Ora sei un pro nell'usare HanuBees.

Per renderlo davvero tuo, ti consigliamo di [scegliere un tema](/docs/themes), [personalizzare i tasti rapidi](/docs/keybinds), [configurare i formatter](/docs/formatters), [creare comandi personalizzati](/docs/commands) o sperimentare con la [configurazione di HanuBees](/docs/config).
